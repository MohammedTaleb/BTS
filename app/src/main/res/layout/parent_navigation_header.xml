<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/popup_bg"
    android:paddingLeft="20px"

    >
    <de.hdodenhof.circleimageview.CircleImageView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/parent_profile_image"
        android:layout_width="75dp"
        android:layout_height="75dp"
        android:src="@drawable/school_logo"
        app:civ_border_width="2dp"
        app:civ_border_color="@color/white"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"

        />
    <TextView
        style="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/parent_profile_image"
        android:layout_centerInParent="true"
        android:layout_marginBottom="29dp"
        android:layout_marginLeft="10dp"
        android:layout_marginStart="10dp"
        android:layout_toEndOf="@+id/parent_profile_image"
        android:layout_toRightOf="@+id/parent_profile_image"
        android:fontFamily="monospace"
        android:text="Ahamad Osama"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold" />
</RelativeLayout>